type PodcastPreview {
  id: String!
  name: String!
  artworkUrl: String!
  itunesUrl: String
  summary: String
  feedUrl: String!
}

extend type Query {
  podcastsPreviews(genreId: String!): [PodcastPreview]
}
