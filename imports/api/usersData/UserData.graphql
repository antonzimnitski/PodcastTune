type UserData {
  _id: String!
  podcasts: [Podcast]
  currentEpisode: Episode
}

extend type Query {
  podcasts: [Podcast]
}

type Mutation {
  subscribe(podcastId: Int!): String
  unsubscribe(podcastId: Int!): String
}
